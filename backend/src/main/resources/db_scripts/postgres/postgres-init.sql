-- PostgreSQL initialization script for OmniAPI (postgres subfolder)
-- This script is automatically executed by PostgreSQL container on first startup
-- Note: Tables are created by Hibernate (DDL), data is loaded by PostgresDatabaseLoader after app starts
-- This script kept minimal to ensure container always starts successfully

-- Optional: Add any database-level initialization here (extensions, schemas, etc.)
-- Currently, all schema and data management is done by Spring Boot

